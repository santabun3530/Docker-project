import React from 'react';

const Header = () => {
  return (
    <header className="app-header">
      <div className="container">
        <h1>CRUD Application</h1>
        <p>A simple application with React, FastAPI, PostgreSQL, and Redis</p>
      </div>
    </header>
  );
};

export default Header;